Sources for the plugin `plugin.fuse-staging`.

Add following to your `build.settings` to use:
```lua
{
    plugins = {
        "plugin.fuse-staging" = {
            publisherId = "com.coronalabs",
        },
    },
}
```
